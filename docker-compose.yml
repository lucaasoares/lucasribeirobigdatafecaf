version: '3.8'

services:
  postgres-iot:
    image: postgres:15
    container_name: postgres-iot
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123456
      POSTGRES_DB: iot_db
    ports:
      - "5432:5432"